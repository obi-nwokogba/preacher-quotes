<%- include(`partials/header.ejs`) %>
<title>Preacher Quotes</title>
</head>

<body>
    <%- include(`partials/navbar.ejs`) %>
    <main class="maindiv">
        <div class="leftdiv">
<h2>Welcome to<br />Preacher Quotes!</h2>
        </div>
        <div class="rightdiv2">
<h2>Preachers</h2>
<div class="rowcontainer">
<% for(let i = 0; i < preachers.length; i++){ %>
       
        <div class="smallpreacherbox">


<div class="preacherthumbnail">
<img src="<%= preachers[i].preacherPhoto %>" class="preacherthumbnail">
</div>


            <h4><a href="/preachers/<%= preachers[i]._id %>"><%= preachers[i].firstName %>  
                <%= preachers[i].lastName %></a></h4>
        </div>
        <% } %>
</div>

<h2>Quotes</h2>
<div class="rowcontainer"></div>

<h2>Members</h2>
<div class="rowcontainer">
        <% for(let i = 0; i < users.length; i++){ %>
                    <h3><a href="/users/<%= users[i]._id %>"><%= users[i].displayName %></a></h3>
                <% } %>
</div>

<%= users.length %>
        </div>
    </main>

    <%- include(`partials/footer.ejs`) %>